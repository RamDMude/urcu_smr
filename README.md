# urcu_smr

To compile the code:

```make```

The default data structure is linkedlist.

For using Queue:

```make clean ```

```make DATASTRUCTURE=QUEUE```

For using Stack:

```make clean ```

```make DATASTRUCTURE=STACK```

To run:

```./rcu_app```

